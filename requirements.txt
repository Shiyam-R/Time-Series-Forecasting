# Rossmann Forecasting API — production runtime dependencies
# Install: pip install -r requirements.txt
# Target interpreter: Python 3.13
#
# This file contains ONLY what the API needs to run in production.
# Test/dev tooling lives in requirements-dev.txt.
#
# Versions verified against real PyPI cp313 wheels for 3.13 compatibility,
# AND against pip-audit for known CVEs (see .github/workflows/ci.yml —
# "Dependency Vulnerability Scan" job). At time of writing, this exact
# set resolves with ZERO known vulnerabilities.
#
#   numpy         2.1.3    (1.26.4's last supported Python is 3.12; this is
#                            a numpy 1.x -> 2.x major bump with documented
#                            breaking changes -- re-validate predictions
#                            after upgrading if you touch this pin)
#   pandas        2.2.3    (first pandas release with 3.13 wheels)
#   scikit-learn  1.5.2    (1.4.2 has no 3.13 wheel)
#   pydantic      2.9.2    (pydantic-core for 2.7.1 has no 3.13 wheel)
#   fastapi       0.139.2  (0.111.0 hard-pins starlette<0.38.0, which carries
#                            9 known CVEs with no available fix in that range;
#                            0.139.2 allows starlette>=0.46.0, resolving to
#                            the patched 1.3.1 -- verified: full test suite
#                            passes against this version, and pip-audit
#                            reports zero findings)
#   python-dotenv 1.2.2    (1.0.1 carries 1 known CVE; trivial patch bump)
#   xgboost       2.0.3    (unchanged -- ships a version-agnostic py3-none
#                            wheel; already works on 3.13, no CVEs found)
#
# python-multipart was REMOVED: it carried 7 known CVEs and was unused —
# no endpoint in this API accepts multipart/form-data (all requests are
# JSON). If a future endpoint needs file uploads, re-add it at a current,
# audited version rather than restoring this pin.

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.139.2
uvicorn[standard]==0.29.0
pydantic==2.9.2

# ── Machine learning ──────────────────────────────────────────────────────────
xgboost==2.0.3
scikit-learn==1.5.2
numpy==2.1.3
pandas==2.2.3

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.2.2      # loads .env file for local dev env-var overrides (no-op
                           # if .env doesn't exist -- safe in Docker/production)