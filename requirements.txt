# Rossmann Forecasting API — production runtime dependencies
# Install: pip install -r requirements.txt
# Target interpreter: Python 3.13
#
# This file contains ONLY what the API needs to run in production.
# Test/dev tooling lives in requirements-dev.txt.
#
# Versions below were verified against real PyPI cp313 wheels (not just
# release notes) to confirm they install cleanly on Python 3.13 with no
# dependency conflicts. Where a package's original pin lacked a 3.13
# wheel, it was bumped to the SMALLEST version that adds one:
#   numpy         1.26.4 -> 2.1.3   (1.26.4's last supported Python is 3.12;
#                                    NOTE: this is a numpy 1.x -> 2.x major
#                                    bump with documented breaking changes
#                                    -- re-validate predictions after upgrading)
#   pandas        2.2.2  -> 2.2.3   (2.2.3 is the first pandas release with 3.13 wheels)
#   scikit-learn  1.4.2  -> 1.5.2   (1.4.2 has no 3.13 wheel; 1.5.2 is the closest that does)
#   pydantic      2.7.1  -> 2.9.2   (pydantic-core for 2.7.1 has no 3.13 wheel; 2.8.0+ does)
#   xgboost       2.0.3  -> unchanged (ships a version-agnostic py3-none wheel;
#                                      already works on 3.13 -- confirms your
#                                      pickled models don't face a version mismatch)

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.9.2

# ── Machine learning ──────────────────────────────────────────────────────────
xgboost==2.0.3
scikit-learn==1.5.2
numpy==2.1.3
pandas==2.2.3

# ── Utilities ─────────────────────────────────────────────────────────────────
python-multipart==0.0.9   # form data support (currently unused by any endpoint —
                           # kept for now, safe to remove later if confirmed unneeded)
python-dotenv==1.0.1      # loads .env file for local dev env-var overrides (no-op
                           # if .env doesn't exist -- safe in Docker/production)