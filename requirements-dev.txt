# Rossmann Forecasting API — development & testing dependencies
# Install: pip install -r requirements-dev.txt
# Target interpreter: Python 3.13
#
# Installs everything in requirements.txt PLUS the tooling needed to
# run the test suite locally and in CI. This file is intentionally
# NOT copied into the Docker image (see .dockerignore) — the
# production container only needs requirements.txt.

-r requirements.txt

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0          # required for FastAPI TestClient

# ── Security scanning (used by CI — see .github/workflows/ci.yml) ────────────
pip-audit==2.7.3